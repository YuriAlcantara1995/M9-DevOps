pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'go build -o sample main.go'
            }
        }
        stage('Save Artifact') {
            steps {
                archiveArtifacts artifacts: 'sample', followSymlinks: false
            }
        }
    }
}
